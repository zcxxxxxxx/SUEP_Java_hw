class car {
    // 写一个car类，具有轮子，车门，车窗，车灯等属性，具有启动，加速，刹车，熄火等方法
    // 要求：
    // 1. 轮子，车门，车窗，车灯等属性用静态变量表示
    // 2. 启动，加速，刹车，熄火等方法用静态方法表示
    // 3. 具有构造方法
    // 4. 在main方法中，创建一个car对象，调用方法

    // 轮子，车门，车窗，车灯等属性用静态变量表示
    static String wheel;
    static String door;
    static String window;
    static String lamp;

    // 启动，加速，刹车，熄火等方法用静态方法表示
    static void start() {
        System.out.println("Car start");
    }

    static void speedUp() {
        System.out.println("Car speed up");
    }

    static void brake() {
        System.out.println("Car brake");
    }

    static void shutDown() {
        System.out.println("Car shut down");
    }

}

class plane {
    // 写一个plane类，具有轮子，机翼，机身，机尾等属性，具有起飞，降落，飞行，停止等方法
    // 要求：
    // 1. 轮子，机翼，机身，机尾等属性用静态变量表示
    // 2. 起飞，降落，飞行，停止等方法用静态方法表示
    // 3. 具有构造方法
    // 4. 在main方法中，创建一个plane对象，调用方法

    // 轮子，机翼，机身，机尾等属性用静态变量表示
    static String wheel;
    static String wing;
    static String body;
    static String tail;

    // 起飞，降落，飞行，停止等方法用静态方法表示
    static void takeOff() {
        System.out.println("Plane take off");
    }

    static void land() {
        System.out.println("Plane land");
    }

    static void fly() {
        System.out.println("Plane fly");
    }

    static void stop() {
        System.out.println("Plane stop");
    }
}

class train {
    // 写一个train类，具有轮子，车头，车身，车尾等属性，具有启动，加速，刹车，熄火等方法
    // 要求：
    // 1. 轮子，车头，车身，车尾等属性用静态变量表示
    // 2. 启动，加速，刹车，熄火等方法用静态方法表示
    // 3. 具有构造方法
    // 4. 在main方法中，创建一个train对象，调用方法

    // 轮子，车头，车身，车尾等属性用静态变量表示
    static String wheel;
    static String head;
    static String body;
    static String tail;

    // 启动，加速，刹车，熄火等方法用静态方法表示
    static void start() {
        System.out.println("Train start");
    }

    static void speedUp() {
        System.out.println("Train speed up");
    }

    static void brake() {
        System.out.println("Train brake");
    }

    static void shutDown() {
        System.out.println("Train shut down");
    }
}

class bike {
    // 写一个bike类，具有轮子，车把，车座，车灯等属性，具有启动，加速，刹车，熄火等方法
    // 要求：
    // 1. 轮子，车把，车座，车灯等属性用静态变量表示
    // 2. 启动，加速，刹车，熄火等方法用静态方法表示
    // 3. 具有构造方法
    // 4. 在main方法中，创建一个bike对象，调用方法

    // 轮子，车把，车座，车灯等属性用静态变量表示
    static String wheel;
    static String handlebar;
    static String seat;
    static String lamp;

    // 启动，加速，刹车，熄火等方法用静态方法表示
    static void start() {
        System.out.println("Bike start");
    }

    static void speedUp() {
        System.out.println("Bike speed up");
    }

    static void brake() {
        System.out.println("Bike brake");
    }

    static void shutDown() {
        System.out.println("Bike shut down");
    }
}